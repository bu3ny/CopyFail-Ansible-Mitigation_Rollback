---
# Pair: copyfail_rollback.yaml — see README “Repository playbooks” for all six entry points.
- name: Apply CopyFail mitigation
  hosts: all
  gather_facts: false
  become: true
  roles:
    - role: copyfail_mitigation
      vars:
        copyfail_mitigation_state: present
