---
# Pair: dirtyfrag_mitigate.yaml — see README “Repository playbooks” for all six entry points.
- name: Roll back Dirty Frag modprobe mitigation
  hosts: all
  gather_facts: false
  become: true
  roles:
    - role: dirtyfrag_mitigation
      vars:
        dirtyfrag_mitigation_state: absent
