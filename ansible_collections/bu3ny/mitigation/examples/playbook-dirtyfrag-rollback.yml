---
# Pair: playbook-dirtyfrag-mitigate.yml — see collection README for all six example playbooks.
- name: Roll back Dirty Frag modprobe mitigation
  hosts: all
  gather_facts: false
  become: true
  collections:
    - bu3ny.mitigation
  roles:
    - role: bu3ny.mitigation.dirtyfrag_mitigation
      vars:
        dirtyfrag_mitigation_state: absent
