# Example inventory — put hosts that may receive the mitigation (and optional auto-reboot)
# under [copyfail_safe_to_reboot]. Addresses below are documentation-only (RFC 5737);
# replace with DNS names or ansible_host= for your environment.

[copyfail_safe_to_reboot]
lab-a ansible_host=203.0.113.10 ansible_user=root
lab-b ansible_host=203.0.113.11 ansible_user=root

[copyfail_manual_reboot]
# Hosts that must NOT be auto-rebooted by the role (optional).

[dirtyfrag_safe_to_reboot]
lab-a ansible_host=203.0.113.10 ansible_user=root
lab-b ansible_host=203.0.113.11 ansible_user=root

[dirtyfrag_manual_reboot]
# Hosts that must NOT be auto-rebooted by Dirty Frag (optional).
